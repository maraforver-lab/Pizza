"use client";

export default function LatestUpdateNotice() {
  // TEMPORARILY DISABLED:
  // The update history and changelog still work, but the site-wide "new feature"
  // banner is hidden while DoughTools is changing frequently during development.
  //
  // Re-enable before/after launch by restoring the visible notice UI.
  return null;
}
